import { create } from "@storybook/theming/create";

const lightTheme = create({
  base: "light",
  brandTitle: "마크업 과제 이수민",
});

const darkTheme = create({
  base: "dark",
  brandTitle: "마크업 과제 이수민",
});

export { darkTheme, lightTheme };
